<ContentDialog
    x:Class="moonlight_xbox_dx.HostActionsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    MinWidth="100" Width="Auto"
    CornerRadius="12"
    BorderBrush="Transparent"
    BorderThickness="0">

    <ContentDialog.Background>
        <AcrylicBrush TintColor="#000"
                      TintOpacity="0.01"
                      FallbackColor="Transparent" />
    </ContentDialog.Background>

    <ContentDialog.Resources>
        <SolidColorBrush x:Key="DialogButtonBackground" Color="#99000000"/>
        <SolidColorBrush x:Key="DialogButtonForeground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="DialogButtonPointerOverBackground" Color="#3A3A3A"/>
        <SolidColorBrush x:Key="DialogButtonPressedBackground" Color="{ThemeResource SystemAccentColor}"/>
        <SolidColorBrush x:Key="DialogButtonFocusedBackground" Color="#EE000000"/>

        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DialogButtonBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource DialogButtonForeground}"/>
            <Setter Property="Padding" Value="8,6,8,10"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="UseSystemFocusVisuals" Value="False"/>
            <Setter Property="FocusVisualPrimaryThickness" Value="0"/>
            <Setter Property="FocusVisualSecondaryThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Padding="0,2">
                            <Border x:Name="BackgroundLayer"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="{TemplateBinding CornerRadius}"/>

                            <ContentPresenter x:Name="ContentPresenter"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              Padding="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Foreground="{TemplateBinding Foreground}"/>

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal">
                                        <VisualState.Setters>
                                            <Setter Target="BackgroundLayer.Background" Value="{StaticResource DialogButtonBackground}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="BackgroundLayer.Background" Value="{StaticResource DialogButtonPointerOverBackground}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="BackgroundLayer.Background" Value="{StaticResource DialogButtonPressedBackground}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="BackgroundLayer.Opacity" Value="0.4"/>
                                            <Setter Target="ContentPresenter.Opacity" Value="0.4"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>

                                <VisualStateGroup x:Name="FocusStates">
                                    <VisualState x:Name="Focused">
                                        <VisualState.Setters>
                                            <Setter Target="BackgroundLayer.Background" Value="{StaticResource DialogButtonFocusedBackground}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unfocused"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ContentDialog.Resources>

    <Grid Background="Transparent" Padding="0">
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Center" Spacing="0">
            <Border CornerRadius="12" Margin="0,0,0,24">

              <Border.Background>
                <AcrylicBrush TintOpacity="0.75" TintLuminosityOpacity="0.25" TintColor="{ThemeResource SystemAccentColor}"/>
              </Border.Background>
              
              <TextBlock x:Name="HostNameHeader"
                          HorizontalAlignment="Center"
                          TextAlignment="Center"
                          FontFamily="Bahnschrift"
                          FontSize="20"
                          FontWeight="SemiBold"
                          Foreground="White"
                          Padding="32,12,32,10"
                          TextWrapping="WrapWholeWords" />
            </Border>

            <Button x:Name="HostDetailsButton" Style="{StaticResource DialogButtonStyle}" Content="Host Details" HorizontalAlignment="Stretch" />
            <Button x:Name="WakeHostButton" Style="{StaticResource DialogButtonStyle}" Content="Wake Host" HorizontalAlignment="Stretch" />
            <Button x:Name="TestConnectionButton" Style="{StaticResource DialogButtonStyle}" Content="Test Connection" HorizontalAlignment="Stretch" />
            <Button x:Name="RemoveHostButton" Style="{StaticResource DialogButtonStyle}" Content="Remove Host" HorizontalAlignment="Stretch" />

            <Border x:Name="ActionsSeparator" Height="1" Opacity="0.0" Background="White" Margin="0,8" />

            <Button x:Name="HostSettingsButton" Style="{StaticResource DialogButtonStyle}" Content="Host Settings" HorizontalAlignment="Stretch" />
            <Button x:Name="MoonlightSettingsButton" Style="{StaticResource DialogButtonStyle}" Content="Moonlight Settings" HorizontalAlignment="Stretch" />
        </StackPanel>
    </Grid>
</ContentDialog>
