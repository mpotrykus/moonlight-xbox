<UserControl
    x:Class="moonlight_xbox_dx.PictureModal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:moonlight_xbox_dx"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Storyboard x:Name="ShowStoryboard">
            <DoubleAnimation Storyboard.TargetName="Root" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2" BeginTime="0:0:0">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Name="HideStoryboard" Completed="HideStoryboard_Completed">
            <DoubleAnimation Storyboard.TargetName="Root" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.18" BeginTime="0:0:0">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseIn"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>

    <Grid x:Name="Root" Visibility="Collapsed" Opacity="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Rectangle Fill="Black" Opacity="0.6" IsHitTestVisible="False" />
        <Border HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="{StaticResource SettingsPanelMaxWidth}" Padding="{StaticResource SettingsPanelInnerMargin}" Style="{StaticResource SettingsGroupStyle}">
            <Border.Background>
                <SolidColorBrush Color="{ThemeResource SystemColorBackgroundColor}" Opacity="0.92" />
            </Border.Background>
            <StackPanel Spacing="4">
                <TextBlock Text="PICTURE" Style="{StaticResource SettingsSectionHeaderStyle}" Margin="4,0,0,12" />

                <Grid RowSpacing="{StaticResource SettingsRowSpacing}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="70" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Contrast" VerticalAlignment="Center" />
                    <Slider x:Name="ContrastSlider" Grid.Row="0" Grid.Column="1" Minimum="50" Maximum="200" StepFrequency="10" Value="100" ValueChanged="ContrastSlider_ValueChanged" VerticalAlignment="Center" />
                    <TextBlock x:Name="ContrastValueText" Grid.Row="0" Grid.Column="2" Text="100%" VerticalAlignment="Center" HorizontalAlignment="Right" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Black Level" VerticalAlignment="Center" />
                    <Slider x:Name="BlackLevelSlider" Grid.Row="1" Grid.Column="1" Minimum="0" Maximum="50" StepFrequency="5" Value="0" ValueChanged="BlackLevelSlider_ValueChanged" VerticalAlignment="Center" />
                    <TextBlock x:Name="BlackLevelValueText" Grid.Row="1" Grid.Column="2" Text="0%" VerticalAlignment="Center" HorizontalAlignment="Right" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="White Level" VerticalAlignment="Center" />
                    <Slider x:Name="WhiteLevelSlider" Grid.Row="2" Grid.Column="1" Minimum="50" Maximum="100" StepFrequency="10" Value="100" ValueChanged="WhiteLevelSlider_ValueChanged" VerticalAlignment="Center" />
                    <TextBlock x:Name="WhiteLevelValueText" Grid.Row="2" Grid.Column="2" Text="100%" VerticalAlignment="Center" HorizontalAlignment="Right" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Gamma" VerticalAlignment="Center" />
                    <Slider x:Name="GammaSlider" Grid.Row="3" Grid.Column="1" Minimum="50" Maximum="200" StepFrequency="10" Value="100" ValueChanged="GammaSlider_ValueChanged" VerticalAlignment="Center" />
                    <TextBlock x:Name="GammaValueText" Grid.Row="3" Grid.Column="2" Text="1.00" VerticalAlignment="Center" HorizontalAlignment="Right" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Saturation" VerticalAlignment="Center" />
                    <Slider x:Name="SaturationSlider" Grid.Row="4" Grid.Column="1" Minimum="0" Maximum="200" StepFrequency="10" Value="100" ValueChanged="SaturationSlider_ValueChanged" VerticalAlignment="Center" />
                    <TextBlock x:Name="SaturationValueText" Grid.Row="4" Grid.Column="2" Text="100%" VerticalAlignment="Center" HorizontalAlignment="Right" />
                </Grid>

                <TextBlock Text="B: Back to menu" FontSize="12" Opacity="0.6" HorizontalAlignment="Center" Margin="0,16,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
